# Server
PORT=8080
LOG_LEVEL=debug

# Database — PostgreSQL connection string
# For local dev, use the docker-compose instance:
DATABASE_URL=postgres://warzone:warzone@localhost:5432/warzone_stats_tracker?sslmode=disable

# Call of Duty API
# Base URL for the unofficial Activision API
COD_API_BASE_URL=https://my.callofduty.com/api/papi-client
# SSO Token — obtain from: my.callofduty.com → DevTools → Cookies → ACT_SSO_COOKIE
# Expires every ~14 days
COD_SSO_TOKEN=your-sso-token-here

# Admin API Key — used to protect admin endpoints (e.g., POST /api/v1/admin/token)
# Generate a secure random string: openssl rand -hex 32
ADMIN_API_KEY=your-admin-api-key-here

# CORS
# Comma-separated allowed origins. Use http://localhost:5173 for local Vue dev server.
CORS_ALLOWED_ORIGINS=http://localhost:5173
