services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: warzone
      POSTGRES_PASSWORD: warzone
      POSTGRES_DB: warzone_stats_tracker
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
